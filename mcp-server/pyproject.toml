[project]
name = "mcp-server"
version = "1.0.0"
description = "The HR skills-matching engine, published over MCP with FastMCP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.4.6",
]
